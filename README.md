# spotify-genre-classifier
Reproducible ML project for genre classification using audio attributes + lyrics (TF-IDF). Modular training/eval scripts, experiment comparison (audio vs lyrics vs fusion), and saved model artifacts for inference.
